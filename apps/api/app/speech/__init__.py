"""Speech-to-text.

Import the submodule — `from app.speech import transcribe` — and call
`transcribe.transcribe(...)`. The package deliberately does not re-export the
function, because a function named `transcribe` would shadow the submodule.
"""
